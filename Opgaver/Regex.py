import re

regex = "([0-9]+)([+-])([0-9]+)"

plus = "21+32"
minus = "21 - 32"
gange = "21 * 32"

print(f"regex: {regex}")

temp = re.split(regex, plus)
print(temp)















class Interpreter:
    pass

class Calculator:
    pass
